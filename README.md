# NLP-Spam-Classification
Building an NLP based SMS Spam detection.

This system would:

Clean and preprocess the text data
Convert all the numeric features using TF-IDF
Perform text analysis to identify important spam keywords
Build a rule-based approach to make predictions on the new text
Build an interactive UI using Streamlit to make the predictions
Run the code: python -m src.main
